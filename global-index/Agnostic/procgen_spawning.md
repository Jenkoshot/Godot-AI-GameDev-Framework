# Procgen Spawning (Agnostic)

This file aggregates logic, scenes, assets, and shaders for this specific feature.

### Debris Burst Spawner
> Two-file system: `DebrisBurstSpawner` spawns a fan of `RigidBody2D` debris fragments radiating

- **Files:**

### Pooled Particle Burst
> Round-robins through a set of pre-instantiated `CPUParticles2D` children instead of instantiating

- **Files:**

